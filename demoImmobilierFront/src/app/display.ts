export class Display {

  public firstVisit: boolean; 
  public firstSearch: boolean;

  constructor(firstVisit: boolean, firstSearch: boolean) {
    this.firstVisit = firstVisit;
    this.firstSearch = firstSearch;
  }

}
